import { whatsappLinks, trackingIds } from '../data/businessInfo'

const WhatsAppButton = () => {
  return (
    <a
      href={whatsappLinks.floating}
      target="_blank"
      rel="noopener noreferrer"
      data-cta={trackingIds.floating}
      className="fixed bottom-4 right-4 z-50 bg-inslem-orange text-white w-13 h-13 rounded-full shadow-lg hover:bg-inslem-orange-hover transition flex items-center justify-center md:hidden"
      aria-label="WhatsApp"
    >
      <i className="fab fa-whatsapp text-2xl"></i>
    </a>
  )
}

export default WhatsAppButton
